# 本课程的 Codex 工作规则

本文件适用于整个仓库。目标是维护一本可阅读、可运行、可操作、可回答、可批改的交互式金融建模教材，并逐步进入量化研究。

## 学生画像

- 本科二年级。
- 已完成 C 语言程序设计、线性代数和高等数学。
- 有一定 Python 接触，但可能忘记语法。
- 没有金融学、概率论、数理统计和计量经济学基础。

任何章节都不得默认学生知道“收益率、贴现、债券、股票、Beta、回归、波动率”等金融或统计术语。术语第一次出现时必须用生活化问题、严格定义和最小数值例子解释。

## 三条课程主线

每个知识点必须同时考虑：

1. 金融概念：问题发生在什么市场情境中？谁向谁提供资金或承担风险？
2. 数学模型：变量、单位、假设、公式和适用边界是什么？
3. Python能力：学生本节实际练习什么语法、库和计算思维？

Python不是独立配角，但也不在章首单列“Python学习目标”。新语法应在小林解决项目问题时自然出现；第一次出现时再就地提供“Python提示”“C/Python对照”或“量化编程警告”。

## 文件职责与权威来源

- `COURSE_REGISTRY.md`：章号、正式名称、Notebook文件名、先修、层级和章间产出的唯一权威来源。
- `README.md`：学生入口和操作方式，不复制完整备课蓝图。
- `COURSE_DESIGN.md`：稳定的18章教学蓝图，不记录频繁变化的执行状态。
- `COURSE_REFERENCES.md`：官方软件文档、监管资料与版本核验入口；不复制整篇来源内容。
- `COURSE_STATE.md`：当前进度、验证证据、薄弱点和下一步，不复制整章讲义。
- Notebook：学生课程内容、回答、错误、输出和修正记录的主版本。
- `tools/`：可复查的维护工具，不是可以覆盖Notebook的另一份“真源”。运行生成或整理脚本前，必须确认它不会覆盖学生或教师的手工修改。

第1、2章的知识性二级标题、数量、顺序和章级主线已经锁定。用户已要求删除章末总结，所以第1章原有总结标题不再属于锁定范围。除非用户再次明确修改，只能在各节内部优化解释、三级标题、提示、练习、图表和反馈。

## Notebook 组织规范

按大章节创建 Notebook，不按单个知识点拆文件。一章可包含多个课时。

学生看到的 Notebook 采用连续项目故事，不出现“学习目标”“AI学习状态”“任务卡”“核心/选学清单”等教案式管理区块。章首直接写清事件、小林的感受和眼前的问题。每个二级标题把故事、知识和行动放在同一个单元格中，不另设重复的故事过渡格。层级、先修和备课判断只写在教师文档中。

每个知识点优先采用下列顺序：

1. 与前章或前节的连接；
2. 一个明确的问题情境，以及本节要替项目解决的唯一麻烦；
3. 运行前预测；
4. 可手算的最小样本；
5. 金融机制、变量、单位和假设；
6. 最小 Python 示例，再进入向量化或库函数；
7. 静态图、滑块或动画及关键状态的普通文本输出；
8. 证据解释：数据直接显示什么、模型假设什么、不能推出什么；
9. 观察问题与 `### 我的回答` 占位区；
10. Python变式、边界测试或练习；
11. AI批改区或项目记录；
12. 用下一件事自然带出后续内容，不另写章末总结。

学生回答区域必须保留：

```markdown
### 我的回答

<!-- 在这里填写；完成前AI不要代答 -->
```

不要删除或覆盖学生的回答、错误代码、实验输出和修正记录。修改已有单元格前，先判断其中是否含有学生内容。

## Python教学规范

- 第一次出现新语法时给出一句用途说明。
- 先给可手算的小样本，再给向量化实现。
- 能用普通循环说明机制时，先展示循环，再对照 NumPy/pandas。
- 不鼓励无法解释的一行式代码。
- 每个核心函数都应说明输入、输出、单位和边界情况。
- 金融时间序列必须显式检查日期排序、缺失值和对齐关系。
- 随机模拟使用 `numpy.random.default_rng(seed)`，并解释随机种子的作用。
- 避免依赖未保存的 Notebook 内核状态；章节应能 Restart & Run All。
- 交互图后尽量输出关键参数和数值摘要，使 Codex 无需观看动画也能检查结果。

## 可视化选择

- 数学公式：Markdown + LaTeX。
- 常规静态图：Matplotlib。
- 参数滑块：ipywidgets；必须提供静态后备方案。
- 悬停与缩放：Plotly，仅在交互确有学习价值时使用。
- 简单动态过程：Matplotlib `FuncAnimation`。
- Manim只用于普通图表无法说明的精细数学动画。

不要为了视觉效果增加动画。可视化必须帮助学生比较、预测或发现模型行为。

可视化分成两类，证据责任不得混淆：

- 生成式概念图：只解释角色、流程和概念关系；Notebook必须配中文读图说明，并注明不承载精确数值。
- 代码生成的数据图：所有坐标、单位、样本、统计量和模型比较均须由明确的Python数据与参数生成；图后应说明横纵轴、可观察事实、假设和不能推出的结论。

## AI互动规则

AI可以解释报错、补充注释、生成测试、提出反例和检查数据泄漏，但不得在学生作答前把完整答案填入回答区。

批改时优先检查：

- 是否解释了变量和单位；
- 是否理解公式的金融含义；
- 是否能预测参数变化；
- 是否混淆相关性、因果和可预测性；
- 是否存在未来信息、样本选择或过拟合；
- 是否能用手算或边界情况验证代码。

若答案错误，先指出具体矛盾并追问，引导学生修正；不要立即覆盖成标准答案。

## 内容与研究规范

- 教学示例不构成投资建议。
- 模拟收益率必须明确标注为假设参数。
- 真实金融事实应优先引用监管机构、交易所、央行、官方文档或原始论文。
- 对可能变化的接口、软件版本、法规和市场规则先联网核实。
- 不以累计收益曲线作为唯一证据；必须讨论成本、风险、样本外表现和模型失效。
- 课程中的机器学习必须使用按时间顺序的验证，禁止随机打乱时序样本后声称得到可交易结论。

## 文件与状态管理

开始新任务时依次读取：

1. 本文件；
2. `COURSE_REGISTRY.md`；
3. `COURSE_STATE.md`；
4. `COURSE_DESIGN.md`；
5. 若任务涉及软件接口、市场规则或术语核验，再读 `COURSE_REFERENCES.md` 并联网复核；
6. 当前 Notebook 的故事进度、最近回答和已保存输出。

每完成一章，更新 `COURSE_STATE.md`。长期状态只保存进度、已掌握内容、常见错误和下一步，不复制整章讲义。

所有生成或修改的 Notebook 都必须通过：

- JSON/Notebook结构检查；
- Python代码语法检查；
- 从头执行检查（学生练习单元可使用安全占位或明确跳过）；
- 图表、中文标签、相对图片链接和单元格顺序检查；
- 第1、2章二级标题锁定检查；
- 学生回答、错误记录、附件和已有输出未被误删的差异检查。
